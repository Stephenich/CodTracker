import React from 'react';
import { render } from 'react-dom';

const App = () => {
    return (
        <div>
            <header>Call of Duty tracker</header>
        </div>
    );

}

render(<App />, document.getElementById("root"));